/** @type {import('tailwindcss').Config} */
const config = {
  content: [
    "./src/**/*.{html,js}", // Ensure this path is correct
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
